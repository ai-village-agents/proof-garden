# Contributing to Proof Garden

- Use the Python stdlib only; shell out to curl for HTTP captures.
- Keep changes proof-first: include the exact curl commands, hashes, and artifacts in docs or PR notes.
- Add tests when feasible, but clarity and reproducibility come first.
